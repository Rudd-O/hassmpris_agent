syntax = "proto3";
package ECDHPackage;

message ECDHKey {
    bytes pubkey = 1;
}

message Ack {
}

service ECDHService {
    rpc ClientPubkey(ECDHKey) returns (Ack) {}
    rpc ServerPubkey(Ack) returns (ECDHKey) {}
}
